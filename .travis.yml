language: ruby

sudo: false

rvm:
  - 2.6
  - 2.5
  - 2.4
  - 2.3

gemfile:
  - gemfiles/activemodel3.gemfile
  - gemfiles/activemodel4.gemfile
  - gemfiles/activemodel5.gemfile

before_install:
  - gem install bundler
